import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // En Vercel, si quieres usar /demo como ruta base, descomenta:
  // basePath: process.env.VERCEL ? "/demo" : "",
  // assetPrefix: process.env.VERCEL ? "/demo" : "",
};

export default nextConfig;
